# PR: uPl-v26b11 — 架构重构：tkinter → PySide6 + 工具链现代化

## 概述

将 ustPlayer 的 GUI 框架从 tkinter 全面迁移至 **PySide6 (Qt for Python)**，同时对代码结构进行了模块化拆分和现代化改造。引入 `uv` 包管理、`setting.json` 集中配置、`build.py` 一键编译。修复了旧代码中的潜在问题。此 PR 是一个完整的底层重写。

---

## 主要变更

### 1. GUI 框架迁移：tkinter → PySide6

| 旧 (v26b10) | 新 (v26b11) |
|---|---|
| `tkinter` / `ttk` | `PySide6.QtWidgets` |
| 手动标签页切换 (`Button` + `Frame`) | `QTabWidget` 原生标签页 |
| 播放器为 `tkinter.Canvas` 绘制 | `QPainter` 高性能渲染 |
| 无类型标注 | `TypedDict` 全类型覆盖 |

### 2. 文件结构重组

```
(旧)                          (新)
ustplayer.py (487行)    →    player.py      (全屏播放器)
ustreader.py (118行)    →    ust_reader.py  (UST解析)
main.py (单文件 1150行)  →    main.py        (主窗口)
                           + ust_types.py   (共享类型定义)
                           + get_setting.py (版本信息读取)
                           + setting.json   (集中配置)
                           + build.py       (Nuitka编译脚本)
                           + pyproject.toml (项目元数据)
                           + uv.lock        (依赖锁定)
```

### 3. 新增：uv 包管理

旧项目无任何依赖管理文件，用户需手动安装 `tkinter`（内置）、`configparser`（内置）后直接运行。本次引入现代 Python 包管理工具 **uv**：

- **`pyproject.toml`**：声明项目元数据及依赖（`pyside6>=6.11.1`, `nuitka>=4.1.1`）
- **`uv.lock`**：锁定所有传递依赖的精确版本，确保可复现构建
- **`.python-version`**：明确 Python >= 3.13，由 uv 自动管理虚拟环境

使用方式：
```bash
uv sync          # 一键安装全部依赖
uv run python main.py   # 直接运行
```

### 4. 新增：setting.json 集中配置

旧版本号硬编码在 `main.py` 的窗口标题中（`self.root.title("ustPlayer - v26b10")`），多处修改容易遗漏。现改为 **`setting.json`** 集中管理：

```json
{
    "Name": "ustPlayer",
    "Version": "26b11"
}
```

`main.py`、`build.py` 等均通过 `get_setting.py` 统一读取，版本号只需改一处。编译时输出文件名也自动同步（`ustPlayer.exe`）。

### 5. 新增：build.py 一键 Nuitka 编译

旧项目依赖 PyInstaller 手动打包，无脚本留存。新增 **`build.py`**：

- 支持 `--onefile`（单文件 .exe）和 `--standalone`（文件夹模式）
- 自动排除未使用的 QtWebEngine 模块（节省约 450 MB）
- 自动读取 `setting.json` 中的 `Name` 作为输出文件名
- 自动检测 `icon.ico` 并嵌入

```bash
python build.py              # 单文件编译
python build.py --standalone  # 目录模式（启动更快）
```

### 6. 现代化工具链补充

- **编译打包**：PyInstaller → `nuitka`（编译为 C，性能更好）
- **类型安全**：新增 `ust_types.py`，所有核心数据结构使用 `TypedDict`，Pyright 静态检查

### 7. 问题代码修复

| 问题 | 旧代码 | 修复 |
|---|---|---|
| 文件未安全关闭 | `ustreader.py` 使用裸 `open()` 无 `with` 上下文管理器 | `ust_reader.py` 使用 `with open(...)` |
| 版本号硬编码分散 | `main.py` 窗口标题、README 等多处单独写版本号 | 统一由 `setting.json` → `get_setting.py` 读取 |
| 类型缺失导致运行时错误 | 所有变量无类型标注，字典键名拼写错误静默失败 | 全量使用 `TypedDict`，Pyright 可在编辑时捕获字段名错误 |
| 图标文件残留 | `icon.ico` 已删除但 README 引用未清理 | 已清理，`build.py` 通过条件判断 `os.path.exists` |
| UST 解析编码处理脆弱 | 旧代码未处理 `UnicodeDecodeError` 的回退逻辑 | 增加 `errors="replace"` 容错，GUI 显式捕获并提示切换编码 |
| 播放器渲染性能 | tkinter Canvas 在大批量音符时掉帧严重 | PySide6 QPainter + QTimer(5ms)，渲染效率显著提升 |
| 线程安全 | 旧代码在子线程中直接操作 tkinter 控件（潜在崩溃） | 播放线程通过 `QTimer` 将 GUI 操作投递到主线程 |

### 8. 新增/修改文件明细

| 文件 | 状态 | 说明 |
|---|---|---|
| `main.py` | 重写 | PySide6 主窗口，QTabWidget 标签页 |
| `player.py` | 新增 | PySide6 全屏播放器，QPainter 渲染 |
| `ust_reader.py` | 重命名+重写 | UST 解析器，TypedDict 返回值 |
| `ust_types.py` | 新增 | `StyleConfig`, `NoteInfo`, `UstInfo` 等类型 |
| `get_setting.py` | 新增 | 从 `setting.json` 读取名称/版本 |
| `setting.json` | 新增 | `{"Name": "ustPlayer", "Version": "26b11"}` |
| `build.py` | 新增 | Nuitka 编译脚本（onefile/standalone） |
| `pyproject.toml` | 新增 | 项目配置，依赖 `pyside6`, `nuitka` |
| `uv.lock` | 新增 | 依赖锁定文件 |
| `.gitignore` | 更新 | 添加 IDE、构建产物等忽略规则 |
| `.python-version` | 新增 | Python 3.13 |
| `ustplayer.py` | **删除** | 旧 tkinter 播放器 |
| `ustreader.py` | **删除** | 旧 UST 解析器（重命名为 `ust_reader.py`） |

### 9. 功能保持

- 所有原有功能完整保留（UST 导入/导出、`.uplr` 工程文件、歌词 LRC、BPM/波形/MIDI 音符显示、颜色自定义等）
- 用户配置 `Settings.ini` 格式向后兼容
- `.uplr` 文件格式不变

---

## 更新内容（v26b11）

- tkinter → PySide6 全面重构，播放器改用 QPainter 渲染，性能大幅提升。
- 引入 uv 包管理，`uv sync` 一键安装依赖。
- 新增 `setting.json` 集中配置，版本号统一管理。
- 新增 `build.py` Nuitka 一键编译脚本。
- 修复文件未安全关闭、线程安全、类型缺失、编码容错等问题。
- 模块化拆分：`player.py` / `ust_reader.py` / `ust_types.py` 各司其职。
- 新增 TypedDict 全类型覆盖，Pyright 静态检查。

---

## 构建说明

```bash
# 安装依赖
uv sync

# 单文件编译
python build.py

# 目录模式编译（启动更快）
python build.py --standalone
```

---

## 版本

**v26b11**（自 v26b10 升级）

## 备注

- 本项目非本人原创，此为技术重构 PR
- 原作者：SYEternalR
- 原项目基于 tkinter 开发
